/// <reference types="lazy-linq-with-types" />
import Db from "./db";
export declare type Tables = {
    [key: string]: IEnumerable<{
        __id: string;
    }>;
};
export declare class DbServer<T> {
    tables: T;
    db: Db<T>;
    constructor(tables: T);
    __reset(): void;
    open(): Promise<Db<T>>;
}
export declare function table<T>(rows: T[]): IEnumerable<T>;
export declare function db<T extends Tables>(tables: T): DbServer<T>;
